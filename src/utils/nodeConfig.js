const ROOT_DIR = __dirname + '../'
const dirPropertiesName = 'test-properties/'; //where to pout the property dir information
const JSON_FILENAME = 'JSON-Fetch-Contentful.json' // for forward compatibility
const imageDir = '/images'

module.exports = {
    dirPropertiesName,
    JSON_FILENAME,
    imageDir
}